# Root of the Lombok config tree; stops it searching parent directories.
config.stopBubbling = true

# Marks generated members with @lombok.Generated so coverage tools skip them
# instead of reporting untested accessors.
lombok.addLombokGeneratedAnnotation = true
