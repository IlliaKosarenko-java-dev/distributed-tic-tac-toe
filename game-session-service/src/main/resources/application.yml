server:
  port: 8082

spring:
  application:
    name: game-session-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
