# Copy to .env and change the values before running anywhere that matters.
# Development defaults are baked into docker-compose.yml so `docker compose up`
# works with no setup; they are not secrets and must not be reused.

MONGO_ROOT_PASSWORD=root-dev-password
MONGO_ENGINE_PASSWORD=engine-dev-password
MONGO_SESSION_PASSWORD=session-dev-password
