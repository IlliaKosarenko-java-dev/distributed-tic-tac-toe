server:
  port: 8080

spring:
  application:
    name: api-gateway

# Routes to the engine and session services are added in phase 7.

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
